<script>
var productType;
var productColor; // Color Options 4/0, 4/1, 4/4
var productQty; // Quantity Options
var productQty_price; // Product price per color option selection

if(productType === "Announcement Enveloopes") {
	// Announcement Envelopes To-Do

	if(productType === "Bookmarks") {
	// Bookmark To-Do

		if(productType === "Brochures"){
		// Brochures To-Do

		
		}
	}
}

</script>